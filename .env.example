# Required
OPENROUTER_API_KEY=

# Optional — model overrides (defaults shown)
SLIDE_MODEL=anthropic/claude-sonnet-4-6
CHAT_MODEL=anthropic/claude-haiku-4-5

# Optional — web search (falls back to DuckDuckGo)
BRAVE_SEARCH_API_KEY=

# Optional — photo search (falls back to placeholder SVG)
UNSPLASH_ACCESS_KEY=

# Optional — AI image generation (falls back to placeholder SVG)
OPENAI_API_KEY=
